production: true
ingress:
  enabled: true
  hostname: "keycloak.dev.6j0.org"
  annotations:
    cert-manager.io/cluster-issuer: "default"
    #nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    #nginx.ingress.kubernetes.io/enable-owasp-core-rules: "true"
    #nginx.ingress.kubernetes.io/limit-rpm: "120"
  tls: true
tls:
  enabled: true
  # TODO: Should we be autogenerating these?
  autoGenerated: true
postgresql:
  enabled: false
externalDatabase:
  host: "keycloak-db-rw"
