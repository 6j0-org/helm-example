replicaCount: 2
image:
  repository: "ghcr.io/6j0-org/website"
  tag: "sha-33a58c3"
imagePullSecrets:
  - name: regcred
service:
  port: 3000
ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: "default-http"
    #nginx.ingress.kubernetes.io/enable-modsecurity: "true"
    #nginx.ingress.kubernetes.io/enable-owasp-core-rules: "true"
    #nginx.ingress.kubernetes.io/limit-rpm: "120"
  hosts:
    - host: website.dev.6j0.org
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: website-tls
      hosts:
        - website.dev.6j0.org
